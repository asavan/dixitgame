const BEGIN_ROUND = 0;
const HIDE = 1;
const MIMIC = 2;
const GUESS = 3;
const COUNT_POINTS = 4;
const ROUND_OVER = 5;

export default {
    BEGIN_ROUND,
    HIDE,
    MIMIC,
    GUESS,
    COUNT_POINTS,
    ROUND_OVER
};
