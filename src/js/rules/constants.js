const BEGIN_ROUND = 0;
const HIDE = 1;
const MIMIC = 2;
const GUESS = 3;
const COUNT_SCORE = 4;
const APPLY_SCORE = 5;
const ROUND_OVER = 6;

export default {
    BEGIN_ROUND,
    HIDE,
    MIMIC,
    GUESS,
    COUNT_SCORE,
    APPLY_SCORE,
    ROUND_OVER
};
